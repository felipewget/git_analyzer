// treated_to_db_repository[i]._id: mongoose.Types.ObjectId = mongoose.Types.ObjectId();
                    // console.log( { ...record_id, ...treated_to_db_repository[i] } )
                    // await new gitRepositoriesModel( { ...record_id, ...treated_to_db_repository[i] } ).save();